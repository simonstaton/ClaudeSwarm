# Core — required
# OpenRouter replaces direct Anthropic API access.
# See: https://openrouter.ai/docs/guides/community/anthropic-agent-sdk
ANTHROPIC_BASE_URL=https://openrouter.ai/api
ANTHROPIC_AUTH_TOKEN=sk-or-v1-...
ANTHROPIC_API_KEY=
API_KEY=your-access-key-to-login
JWT_SECRET=any-random-string-for-local-dev

# GCS — optional for local dev (persistence disabled without it)
# GCS_BUCKET=your-project-claude-swarm-data

# Shared context — local dev uses a local directory
SHARED_CONTEXT_DIR=./shared-context

# MCP servers — optional, only activated if set
# NOTION_API_KEY=ntn_...
# GITHUB_TOKEN=ghp_...  # or fine-grained: github_pat_... — enables gh CLI + git push
# SLACK_TOKEN=xoxb-...
# GOOGLE_CREDENTIALS=...
# FIGMA_TOKEN=figd_...   # required for Figma MCP tools
# LINEAR_API_KEY=lin_api_...  # required for Linear MCP tools

# Server
PORT=8080

# CORS — optional, only needed if UI is hosted on a different origin
# Set to * to allow any origin, or comma-separated origins for fine-grained control
# Not set = same-origin only (default, works behind Cloud Run's proxy)
# CORS_ORIGINS=*
# CORS_ORIGINS=https://app.example.com,https://staging.example.com
